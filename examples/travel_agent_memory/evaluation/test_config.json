{
  "criteria": [
    {
      "name": "tool_trajectory_avg_score",
      "description": "Checks if the agent uses the expected tools in the correct order",
      "weight": 0.3
    },
    {
      "name": "final_response_match_v2",
      "description": "Uses LLM to judge if the final response semantically matches the expected response",
      "weight": 0.4,
      "config": {
        "model": "gemini-2.0-flash-exp",
        "threshold": 0.7
      }
    },
    {
      "name": "rubric_based_tool_use_quality_v1",
      "description": "Evaluates the quality of tool usage based on custom rubrics",
      "weight": 0.3,
      "config": {
        "rubric": [
          "The agent should use memory tools (search_memory, create_memory) when appropriate",
          "The agent should search memory before creating duplicate entries",
          "The agent should use web_search only when current information is needed",
          "The agent should use export_to_calendar when user requests calendar export"
        ]
      }
    }
  ],
  "default_model": "gemini-2.0-flash-exp",
  "timeout_seconds": 30
}

